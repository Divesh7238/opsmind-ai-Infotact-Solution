# Client Environment Variables
# Copy this to .env and adjust as needed

# API Base URL
# Local dev can use /api with the Vite proxy.
# Hosted deployments can use either the server root URL or the full /api URL.
VITE_API_URL=/api

